{
  "buildCommand": "cd enprotec-operations && npm install && npm run build",
  "outputDirectory": "enprotec-operations/dist",
  "installCommand": "echo skip",
  "rewrites": [
    {
      "source": "/reset-password",
      "destination": "/index.html"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
